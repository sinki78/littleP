{% extends 'base.html.twig' %}

{% block body %}
    <h1>Dodo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dodo.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ dodo.nom }}</td>
            </tr>
            <tr>
                <th>Nbpersonne</th>
                <td>{{ dodo.nbpersonne }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('dodo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('dodo_edit', { 'id': dodo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
