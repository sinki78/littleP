{% extends 'base.html.twig' %}

{% block body %}
    <h1>Passager</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ passager.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ passager.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('passager_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('passager_edit', { 'id': passager.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
